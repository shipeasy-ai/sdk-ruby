=begin
#Shipeasy Admin API

#REST API for managing feature gates, experiments, configs, universes, and killswitches in a Shipeasy project. Authenticate with an admin SDK key (`Authorization: Bearer sdk_admin_…`) and scope every request to a project via the `X-Project-Id` header.  Mint admin keys via `POST /api/admin/keys` with `type: \"admin\"`. Keys expire after 90 days; rotate with the `revoke` action.

The version of the OpenAPI document: 2.0.0

Generated by: https://openapi-generator.tech
Generator version: 7.23.0

=end

require 'date'
require 'time'

module Shipeasy::Admin::Generated
  # Non-secret config for a Cursor trigger.
  class CursorTriggerConfig < ApiModelBase
    # **First** repo the cloud agent runs against, e.g. `https://github.com/owner/repo`. Idempotency key for the connector, and always a mirror of `repos[0].url` when `repos` is supplied.
    attr_accessor :repo_url

    # Optional git ref the run starts from. Mirrors `repos[0].startingRef`. Pinned onto the Cursor agent at creation, so changing it provisions a replacement agent.
    attr_accessor :starting_ref

    # Every repo this trigger runs against, each with its own starting branch — Cursor launches ONE agent across the whole set. Omit for a single-repo trigger, which is then described by `repoUrl`/`startingRef` alone; when supplied, the first entry must match them. Changing the set re-provisions the agent (its repos are pinned at creation).
    attr_accessor :repos

    # Shipeasy project id the run targets.
    attr_accessor :project_id

    # Server-managed. The reusable Cursor cloud agent (`bc-…`) Shipeasy provisions once at connect and then starts every run on. A supplied value is ignored on create; if Cursor stops recognising the id (404), the next fire provisions a replacement and stores it here.
    attr_accessor :agent_id

    # Optional standing instruction prepended to every run's prompt — the trigger's base prompt. Lets two triggers on different repos steer their runs differently without changing the `/shipeasy-ops-work` task block.
    attr_accessor :fire_text

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        :'repo_url' => :'repoUrl',
        :'starting_ref' => :'startingRef',
        :'repos' => :'repos',
        :'project_id' => :'projectId',
        :'agent_id' => :'agentId',
        :'fire_text' => :'fireText'
      }
    end

    # Returns attribute mapping this model knows about
    def self.acceptable_attribute_map
      attribute_map
    end

    # Returns all the JSON keys this model knows about
    def self.acceptable_attributes
      acceptable_attribute_map.values
    end

    # Attribute type mapping.
    def self.openapi_types
      {
        :'repo_url' => :'String',
        :'starting_ref' => :'String',
        :'repos' => :'Array<CursorTriggerRepo>',
        :'project_id' => :'String',
        :'agent_id' => :'String',
        :'fire_text' => :'String'
      }
    end

    # List of attributes with nullable: true
    def self.openapi_nullable
      Set.new([
      ])
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      if (!attributes.is_a?(Hash))
        fail ArgumentError, "The input argument (attributes) must be a hash in `Shipeasy::Admin::Generated::CursorTriggerConfig` initialize method"
      end

      # check to see if the attribute exists and convert string to symbol for hash key
      acceptable_attribute_map = self.class.acceptable_attribute_map
      attributes = attributes.each_with_object({}) { |(k, v), h|
        if (!acceptable_attribute_map.key?(k.to_sym))
          fail ArgumentError, "`#{k}` is not a valid attribute in `Shipeasy::Admin::Generated::CursorTriggerConfig`. Please check the name to make sure it's valid. List of attributes: " + acceptable_attribute_map.keys.inspect
        end
        h[k.to_sym] = v
      }

      if attributes.key?(:'repo_url')
        self.repo_url = attributes[:'repo_url']
      else
        self.repo_url = nil
      end

      if attributes.key?(:'starting_ref')
        self.starting_ref = attributes[:'starting_ref']
      end

      if attributes.key?(:'repos')
        if (value = attributes[:'repos']).is_a?(Array)
          self.repos = value
        end
      end

      if attributes.key?(:'project_id')
        self.project_id = attributes[:'project_id']
      else
        self.project_id = nil
      end

      if attributes.key?(:'agent_id')
        self.agent_id = attributes[:'agent_id']
      end

      if attributes.key?(:'fire_text')
        self.fire_text = attributes[:'fire_text']
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      warn '[DEPRECATED] the `list_invalid_properties` method is obsolete'
      invalid_properties = Array.new
      if @repo_url.nil?
        invalid_properties.push('invalid value for "repo_url", repo_url cannot be nil.')
      end

      if !@starting_ref.nil? && @starting_ref.to_s.length < 1
        invalid_properties.push('invalid value for "starting_ref", the character length must be greater than or equal to 1.')
      end

      if !@repos.nil? && @repos.length < 1
        invalid_properties.push('invalid value for "repos", number of items must be greater than or equal to 1.')
      end

      if @project_id.nil?
        invalid_properties.push('invalid value for "project_id", project_id cannot be nil.')
      end

      if @project_id.to_s.length < 1
        invalid_properties.push('invalid value for "project_id", the character length must be greater than or equal to 1.')
      end

      if !@agent_id.nil? && @agent_id.to_s.length < 1
        invalid_properties.push('invalid value for "agent_id", the character length must be greater than or equal to 1.')
      end

      if !@fire_text.nil? && @fire_text.to_s.length < 1
        invalid_properties.push('invalid value for "fire_text", the character length must be greater than or equal to 1.')
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      warn '[DEPRECATED] the `valid?` method is obsolete'
      return false if @repo_url.nil?
      return false if !@starting_ref.nil? && @starting_ref.to_s.length < 1
      return false if !@repos.nil? && @repos.length < 1
      return false if @project_id.nil?
      return false if @project_id.to_s.length < 1
      return false if !@agent_id.nil? && @agent_id.to_s.length < 1
      return false if !@fire_text.nil? && @fire_text.to_s.length < 1
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] repo_url Value to be assigned
    def repo_url=(repo_url)
      if repo_url.nil?
        fail ArgumentError, 'repo_url cannot be nil'
      end

      @repo_url = repo_url
    end

    # Custom attribute writer method with validation
    # @param [Object] starting_ref Value to be assigned
    def starting_ref=(starting_ref)
      if starting_ref.nil?
        fail ArgumentError, 'starting_ref cannot be nil'
      end

      if starting_ref.to_s.length < 1
        fail ArgumentError, 'invalid value for "starting_ref", the character length must be greater than or equal to 1.'
      end

      @starting_ref = starting_ref
    end

    # Custom attribute writer method with validation
    # @param [Object] repos Value to be assigned
    def repos=(repos)
      if repos.nil?
        fail ArgumentError, 'repos cannot be nil'
      end

      if repos.length < 1
        fail ArgumentError, 'invalid value for "repos", number of items must be greater than or equal to 1.'
      end

      @repos = repos
    end

    # Custom attribute writer method with validation
    # @param [Object] project_id Value to be assigned
    def project_id=(project_id)
      if project_id.nil?
        fail ArgumentError, 'project_id cannot be nil'
      end

      if project_id.to_s.length < 1
        fail ArgumentError, 'invalid value for "project_id", the character length must be greater than or equal to 1.'
      end

      @project_id = project_id
    end

    # Custom attribute writer method with validation
    # @param [Object] agent_id Value to be assigned
    def agent_id=(agent_id)
      if agent_id.nil?
        fail ArgumentError, 'agent_id cannot be nil'
      end

      if agent_id.to_s.length < 1
        fail ArgumentError, 'invalid value for "agent_id", the character length must be greater than or equal to 1.'
      end

      @agent_id = agent_id
    end

    # Custom attribute writer method with validation
    # @param [Object] fire_text Value to be assigned
    def fire_text=(fire_text)
      if fire_text.nil?
        fail ArgumentError, 'fire_text cannot be nil'
      end

      if fire_text.to_s.length < 1
        fail ArgumentError, 'invalid value for "fire_text", the character length must be greater than or equal to 1.'
      end

      @fire_text = fire_text
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          repo_url == o.repo_url &&
          starting_ref == o.starting_ref &&
          repos == o.repos &&
          project_id == o.project_id &&
          agent_id == o.agent_id &&
          fire_text == o.fire_text
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [repo_url, starting_ref, repos, project_id, agent_id, fire_text].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      attributes = attributes.transform_keys(&:to_sym)
      transformed_hash = {}
      openapi_types.each_pair do |key, type|
        if attributes.key?(attribute_map[key]) && attributes[attribute_map[key]].nil?
          transformed_hash["#{key}"] = nil
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[attribute_map[key]].is_a?(Array)
            transformed_hash["#{key}"] = attributes[attribute_map[key]].map { |v| _deserialize($1, v) }
          end
        elsif !attributes[attribute_map[key]].nil?
          transformed_hash["#{key}"] = _deserialize(type, attributes[attribute_map[key]])
        end
      end
      new(transformed_hash)
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.include?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

  end

end
